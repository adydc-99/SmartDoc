spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/smartdoc?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai}
    username: ${DB_USERNAME:smartdoc}
    password: ${DB_PASSWORD:smartdoc}
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never
